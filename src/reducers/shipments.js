const shipmentsReducerDefaultState = {
  data: [],
  dataFilter: [],
  isLoading: true
}

export default (state = shipmentsReducerDefaultState, action ) => {
  switch(action.type) {
    default:
      return state
  }
}